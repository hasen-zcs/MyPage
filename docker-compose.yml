name: notehub

services:
  notehub:
    build: .
    ports:
      - "8000:8000"
    environment:
      HOST: 0.0.0.0
      PORT: 8000
    volumes:
      - ./notes:/app/notes
      - ./data:/app/data
    restart: unless-stopped
